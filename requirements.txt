Pillow>=9.0
numpy>=1.22
# CA bundle for HTTPS. The frozen .exe ships no system certs, so urllib's
# update check to api.github.com fails with CERTIFICATE_VERIFY_FAILED without
# this; updater.py points its SSL context at certifi.where().
certifi>=2023.0
# Optional — enables drag-and-drop PNG import from the OS file manager.
# The engine still runs without it, drag-drop just won't work.
tkinterdnd2>=0.3
